---
import { gradients } from '@/scripts/gradients.ts';

type Props = {
    c: keyof typeof gradients;
};

const { c, ...props } = Astro.props;
---
<strong class:list={[
    'bg-clip-text !text-transparent drop-shadow-sm',
    gradients[c],
]} {...props}><slot /></strong>